import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-basic-1904333',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
